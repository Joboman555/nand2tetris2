/** Initializes a new HexLife game and starts running it. */
class Main {
    function void main() {
        var HexLifeGame game;
        let game = HexLifeGame.new();
        do game.run();
        do game.dispose();
        return;
    }
}
