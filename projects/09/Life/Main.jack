/** Initializes a new game of life and starts running it. */
class Main {
    function void main() {
        var GameOfLife game;
        let game = GameOfLife.new();
        do game.run();
        do game.dispose();
        return;
    }
}
